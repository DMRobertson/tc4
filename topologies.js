var Topology = {
	name: 'name of Topology',
	addEdges: function (container, width, height) {
		// draw the edges of the board and indicate the chosen topology
		// return nothing
	},
	getRay: function (tiles, direction, x, y) {
		// return a length-3 array of indices showing which tiles can be used to connect-four with (x, y) in the given direction
	}
};